//----------- paper size
boolean finished = false;
while (!finished) {
	print ">: ";
	String paper = System.console().readLine();
	
	char ch = paper.charAt(0) 
	if(ch=='A' || ch=='a')
		finished = true;
	else
		println "Incorrect input, please begin with A";
}

