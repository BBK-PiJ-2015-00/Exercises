print (">: ");
String input2 = System.console().readLine();

int marker = 0;
for (int i = 0; i < input2.length(); i++) {
	if (input2.charAt(i) == ' ') {
		println(input2.substring(marker, i));
		marker = i+1; 
	}
}

println input2.substring(marker, input2.length());